# Set default behavior, in case people don't have core.autocrlf set.
* text=auto

# Ensure batch scripts always have CRLF line endings
*.bat text eol=crlf
*.cmd text eol=crlf

# Ensure bash scripts always have LF line endings
*.sh text eol=lf
